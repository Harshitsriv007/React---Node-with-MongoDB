# React---Node-with-MongoDB

Install.

``` bash
npm install
```

Run the Node server.

``` bash
npm run dev
```

Run dev server, clients can be requested on localhost:3001.

``` bash
npm run client
```
